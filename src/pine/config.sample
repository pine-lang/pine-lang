(ns pine.config)

(let [name "information_schema"]
  (def config {:classname "com.mysql.jdbc.Driver"
               :subprotocol "mysql"
               :subname (str "//host.docker.internal:3306/" name)
               :user "?"
               :password "?"
               :name name}))
